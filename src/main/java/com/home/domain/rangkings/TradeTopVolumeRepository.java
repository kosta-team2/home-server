package com.home.domain.rangkings;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TradeTopVolumeRepository extends JpaRepository<TradeTopVolume, TradeTopVolumeId> {
}
